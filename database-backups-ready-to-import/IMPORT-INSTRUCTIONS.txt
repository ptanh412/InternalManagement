╔══════════════════════════════════════════════════════════════════════════╗
║              🗄️  DATABASE IMPORT INSTRUCTIONS                            ║
║          Internal Management System - Ready to Import Backup             ║
╚══════════════════════════════════════════════════════════════════════════╝

📦 PACKAGE CONTENTS:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
├── mysql/all_databases.sql              (4.3 MB)
│   └── 4 databases: identity, project, task, workload
│
├── neo4j/neo4j.dump                     (40 KB)
│   └── Graph database with user/project relationships
│
├── mongodb/mongodb-backup/               (4.1 MB)
│   └── 8 databases: ai_service, chat-service, file-service,
│       notification-service, post-service, admin, config, local
│
├── postgresql/ml_training_db.dump       (41 MB)
│   └── ML training database with models and predictions
│
└── README.md                            (Detailed documentation)

TOTAL SIZE: ~49.4 MB


🚀 QUICK START - ONE COMMAND IMPORT:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

From project root directory, run:

    ./import-databases-from-backup.sh

This will automatically import ALL databases with progress tracking.


📝 MANUAL IMPORT (Step by Step):
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1️⃣  MySQL (4.3 MB - 4 databases)
────────────────────────────────────
docker exec -i mysql-oracle sh -c 'exec mysql -uroot -p123456' \
  < database-backups-ready-to-import/mysql/all_databases.sql

✓ Imports: identity, project, task, workload databases
✓ Time: ~10 seconds


2️⃣  Neo4j (40 KB - Graph database)
────────────────────────────────────
docker stop neo4j
docker cp database-backups-ready-to-import/neo4j/neo4j.dump neo4j:/tmp/
docker exec neo4j neo4j-admin database load --from-path=/tmp neo4j \
  --overwrite-destination=true
docker start neo4j

✓ Imports: All nodes and relationships
✓ Time: ~20 seconds (including restart)
✓ Access: http://localhost:7474 (neo4j/12345678)


3️⃣  MongoDB (4.1 MB - 8 databases)
────────────────────────────────────
docker cp database-backups-ready-to-import/mongodb/mongodb-backup mongodb:/tmp/
docker exec mongodb mongorestore --drop /tmp/mongodb-backup
docker exec mongodb rm -rf /tmp/mongodb-backup

✓ Imports: ai_service, chat-service, file-service, notification-service,
          post-service, admin, config, local
✓ Time: ~15 seconds
✓ Collections: 16 collections, 4,113 documents


4️⃣  PostgreSQL (41 MB - ML database)
────────────────────────────────────
docker cp database-backups-ready-to-import/postgresql/ml_training_db.dump \
  ml-postgres:/tmp/
docker exec ml-postgres pg_restore -U ml_user -d ml_training_db -c -F c \
  /tmp/ml_training_db.dump
docker exec ml-postgres rm /tmp/ml_training_db.dump

✓ Imports: ml_training_db with training data
✓ Time: ~30 seconds


⏱️  TOTAL IMPORT TIME: ~1-2 minutes
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━


✅ VERIFICATION COMMANDS:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

MySQL:
  docker exec mysql-oracle mysql -uroot -p123456 -e "SHOW DATABASES;"

Neo4j:
  Open http://localhost:7474 in browser
  Run: MATCH (n) RETURN count(n);

MongoDB:
  docker exec mongodb mongosh --eval "show dbs"

PostgreSQL:
  docker exec ml-postgres psql -U ml_user -d ml_training_db -c "\dt"


👤 TEST ACCOUNTS (After MySQL Import):
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Frontend: http://localhost:3000

Username: admin     │ Password: admin    │ Role: ADMIN
Username: victor    │ Password: 123456   │ Role: USER
Username: iris      │ Password: 123456   │ Role: MANAGER
Username: henry     │ Password: 123456   │ Role: USER


⚠️  IMPORTANT NOTES:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✓ All Docker containers must be running before import
✓ Neo4j requires container stop/start during import
✓ MongoDB has no authentication configured (empty user list)
✓ PostgreSQL user is "ml_user", not "postgres"
✓ Import will DROP existing data (backup first if needed)


🔧 TROUBLESHOOTING:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Container not running:
  docker start mysql-oracle neo4j mongodb ml-postgres

MySQL access denied:
  docker exec mysql-oracle mysql -uroot -p123456 -e "FLUSH PRIVILEGES;"

Neo4j database locked:
  docker stop neo4j && sleep 5 && docker start neo4j

PostgreSQL database busy:
  docker exec ml-postgres psql -U ml_user -d postgres -c \
    "SELECT pg_terminate_backend(pid) FROM pg_stat_activity \
     WHERE datname='ml_training_db';"


📚 MORE INFORMATION:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

See README.md in this folder for:
  • Detailed database information
  • Advanced import options
  • Database connection strings
  • Complete troubleshooting guide


═══════════════════════════════════════════════════════════════════════════
Created: 30/12/2025
System: Internal Management - Microservices Platform
Databases: MySQL 8.0, Neo4j, MongoDB 7.0.11, PostgreSQL 15
═══════════════════════════════════════════════════════════════════════════
